
a = 4
x = [1, 2]

switch a:
	case 1:
		print([1, 2, 3])
	case 2:
		print([4, 5, 6])
	default:
		print(x + a)
	
prim = [2]

for e in range(3, 100):
	div = 1
	for p in prim:
		if not (e % p):
			div = 0
			break
	if div:
		add(prim, e)

print(prim)
